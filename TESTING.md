# 测试指南

## 测试样本准备

为了全面测试插件功能，建议准备以下测试样本：

### 1. 标准 LIVP（1 JPG + 1 MOV）
- 包含一个 JPG 图片和一个 MOV 视频
- 验证缩略图和预览功能正常

### 2. 多 JPG（多个 JPG + 1 MOV）
- 包含多个 JPG 文件和一个 MOV
- 验证选择最大 JPG 的逻辑

### 3. 多 MOV（1 JPG + 多个 MOV）
- 包含一个 JPG 和多个 MOV 文件
- 验证选择最大 MOV 的逻辑

### 4. 仅 JPG（只有 JPG，无 MOV）
- 只包含 JPG 文件
- 验证缩略图生成正常，预览只显示静态图

### 5. 仅 MOV（只有 MOV，无 JPG）
- 只包含 MOV 文件
- 验证缩略图生成占位图，预览显示占位

### 6. 非 ZIP（损坏或非 ZIP 格式）
- 损坏的 .livp 文件或非 ZIP 格式
- 验证错误处理和占位图生成

## 测试步骤

1. **安装插件**
   - 将插件目录复制到 Eagle 插件目录
   - 运行 `npm install` 安装依赖
   - 在 Eagle 中启用插件

2. **测试缩略图生成**
   - 导入 .livp 文件到 Eagle
   - 检查列表/瀑布流中是否显示缩略图
   - 检查缩略图是否清晰、比例正确

3. **测试预览功能**
   - 双击打开 .livp 文件
   - 检查静态图片是否正常显示
   - 检查视频是否可以播放（如果系统支持）
   - 测试"显示视频"/"隐藏视频"按钮

4. **测试错误处理**
   - 使用异常样本测试
   - 检查是否生成占位图
   - 检查日志文件（`logs/thumbnail.log`）

## 调试

由于 Format Extension 不支持 DevTools，调试主要通过：

1. **日志文件**
   - 查看 `logs/thumbnail.log` 了解缩略图生成错误
   - 查看浏览器控制台（如果可用）了解预览错误

2. **占位图**
   - 如果看到占位图，说明处理过程中出现错误
   - 检查日志文件了解具体错误信息

3. **文件检查**
   - 确认 .livp 文件确实是 ZIP 格式
   - 使用解压工具检查 ZIP 内容

## 已知限制

- Windows 下 MOV 播放依赖系统解码器，可能不稳定
- 大文件处理可能需要较长时间
- 不支持非 ZIP 格式的 LIVP 变体
